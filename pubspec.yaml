name: flutter_transaction_tracker
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  provider: ^6.0.5
  google_fonts: ^5.1.0

dev_dependencies:
  integration_test:
    sdk: flutter
  test: ^1.16.8
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
